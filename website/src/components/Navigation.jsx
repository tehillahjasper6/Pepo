// Canonical Navigation is at `src/components/layout/Navigation.jsx`.
// Re-export it here to avoid duplicate header components in the build.

export { default } from './layout/Navigation'
  const [isOpen, setIsOpen] = useState(false)

